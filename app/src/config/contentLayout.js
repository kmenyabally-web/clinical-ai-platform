/**
 * Default max width for main clinical shells (patient record, forms).
 * Wide enough for large desktops; list/table screens often use full main width instead.
 */
export const CLINICAL_CONTENT_MAX_WIDTH_PX = 1920;
